import 'package:flutter/material.dart';

class AppColors {
  // Primary Palette
  static const Color primary = Color(0xFF008080);
  static const Color primaryLight = Color(0xFF4FB6B6);
  static const Color primaryDark = Color(0xFF005F5F);

  // Secondary Palette
  static const Color secondary = Color(0xFF004080);
  static const Color secondaryLight = Color(0xFF4D79A6);
  static const Color secondaryDark = Color(0xFF002B57);

  // Tertiary
  static const Color tertiary = Color(0xFFA96039);

  // Neutral / Background
  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textMuted = Color(0xFF9CA3AF);

  // Status colors
  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
}